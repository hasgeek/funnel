<div class="infosheet">
    <div class="section first">
        <div class="row">
            <button type="button" class="close pull-right" data-dismiss="modal" aria-hidden="true">&times;</button>
            <h1>{{ session.title }}</h1>
            {% if session.speaker %}
                <h4><small>by</small> {{ session.speaker }}</h4>
            {% endif %}
            <h3>
                <small>at</small>
                {{ localize_date(session.start, to_tz=timezone).strftime('%I:%M %p') }} - {{ localize_date(session.end, to_tz=timezone).strftime('%I:%M %p') }}
                <small>on</small>
                {{ localize_date(session.start, to_tz=timezone).strftime('%d %B, %Y') }}
            </h3>
            {% if session.proposal %}
                <a class="pull-right btn btn-info btn-xs" href="{{ session.proposal.url_for('view') }}" target="_blank">View Proposal</a>
            {% endif %}
            {% if session.venue_room %}
                <h4><small>in</small> {{ session.venue_room.title }} <small>{{ session.venue_room.venue.title }}</small></h4>
            {% endif %}
        </div>
    </div>
    <div class="section bar">
        <dl class="bar">
            {% if session.proposal %}
                <br class="hide-desktop">
                {% if session.proposal.section %}
                    <dt>Section</dt><dd>{{ session.proposal.section.title }}</dd>
                {% endif %}
                <dt>Technical Level</dt><dd>{{ session.proposal.technical_level }}
            {% endif %}
        </dl>
    </div>
    <div class="section">
        {% if session.description_html %}
            <h2>{% trans %}Description{% endtrans %}</h2>
            {{ session.description_html|safe }}
        {% endif %}
        {% if session.speaker_bio_html %}
            <h2>{% trans %}Speaker Bio{% endtrans %}</h2>
            {{ session.speaker_bio_html|safe }}
        {% endif %}
    </div>
</div>